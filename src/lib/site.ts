// Site-wide constants used by the Seo component and JSON-LD blocks.
// SITE_URL is the canonical custom domain (see static/CNAME) — used to
// build absolute URLs for canonical links, Open Graph, and schema.org
// data, which all require full URLs rather than relative paths.
export const SITE_URL = 'https://joelparkerhenderson.com';
export const SITE_NAME = 'Joel Parker Henderson';
export const DEFAULT_OG_IMAGE = `${SITE_URL}/images/og-default.jpg`;
